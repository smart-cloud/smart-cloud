spring:
  rabbitmq:
    host: localhost
    port: 5672
    virtualHost: dev_activity_vhost
  redis:
    database: 1
    host: localhost
    port: 6379
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        max-wait: -1ms
        min-idle: 0