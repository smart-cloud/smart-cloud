server:
  port: 40004
spring:
  application:
    name: ratelimit

smart:
  rate-limit:
    config:
      'io.github.smart.cloud.starter.rate.limit.test.prepare.controller.ProductController.query': 20
      'io.github.smart.cloud.starter.rate.limit.test.prepare.controller.ProductController.create': 1